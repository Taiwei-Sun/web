tcl

#!/usr/bin/expect -f

set a1 [lindex $argv 0 ]

if {[info exists out]} {
    puts "variable does exist"
}


if {[llength $a2] == 0} then {
    puts "variable does not exist"
}

spawn ssh user@hostname

spawn ssh 後面不能有連續空白