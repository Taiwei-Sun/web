定義array: var A=[1,2,3];
for loop: for (i = 0; i < cars.length; i++) {process;}


error
var bodyLI=document.body.getElementsByTagName("LI");
/*
for(i=0;i<bodyLI.length;i++){
 //pp.innerHTML=pp.innerHTML+" "+bodyLI[i].nodeName;
 tmp=bodyLI[i];
 tmp.onmouseover=function(){itemPlay(tmp)};
 tmp.onmouseout=function(){itemDisplay(tmp)};
}
*/

/*
tmp=bodyLI[0];
tmp.onmouseover=function(){itemPlay(tmp)};
tmp.onmouseout=function(){itemDisplay(tmp)};

tmp=bodyLI[1];
tmp.onmouseover=function(){itemPlay(tmp)};
tmp.onmouseout=function(){itemDisplay(tmp)};
*/


未定義參數  A==null  A===undefind

e.target 可以抓到觸發事件的物件


設定main的頁匡轉換網頁
function changeMainPage(urlStr){
 var a = document.createElement('a');
 a.href=urlStr;
 a.target = 'main';var bodyLI=document.body.getElementsByTagName("LI");
  document.body.appendChild(a);
 a.click();
 document.body.removeChild(a);
}


document.createElement("p");
document.createTextNode("This is new.");
element.appendChild(node);
element.insertBefore(para, child);


var str = "Visit Microsoft! Microsoft!";
var res = str.replace("Microsoft", "W3Schools");//改變第一個找到的
var res = str.replace(/Microsoft/g, "W3Schools");//改變全部找到的


document.body.getElementById("mainUL");

return false;
e.stopPropagation();
if(e.target!==this)return;


    var p=document.getElementById("myLI");
    var q=p.parentNode;
    var x = q.nodeName;
    document.getElementById("demo").innerHTML = x;
    //p.parentNode.removeChild(p);//not work
    q.removeChild(p);



img參數x y有可能會被瀏覽器輸入現在的位置


//改變class會直接改變已經用class抓到的變數
var groupC=document.getElementsByClassName(oldC);
groupC[i].classList.add(newC);//不會改變到
document.test.innerHTML=document.test.innerHTML+" 1.groupC[0].className="+groupC[0].className;//成功
groupC[i].className=newC;//會改變到groupC變數
document.test.innerHTML=document.test.innerHTML+" 1.groupC[0].className="+groupC[0].className;//失敗
